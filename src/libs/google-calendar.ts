// TODO: Google Calendar API v3 래퍼
// - listEvents(timeMin, timeMax, options): 일정 목록 조회
// - getEvent(eventId): 일정 상세 조회
// - createEvent(eventData): 일정 생성
// - updateEvent(eventId, eventData): 일정 수정
// - deleteEvent(eventId): 일정 삭제
// - queryFreeBusy(timeMin, timeMax): 빈 시간 조회
// - syncToCache(): Google → calendar_cache 동기화
